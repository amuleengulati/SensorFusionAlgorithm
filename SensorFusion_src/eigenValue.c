#include<<gsl/gsl_matrix.h>
#include <gsl/gsl_math.h>
#include <gsl/gsl_eigen.h>
 
 double* eigenValue(double sdm[][] , int n){
	 
	 for(int i = 0;i<n;i++){
		 //compute eigen values lambda1, lambda2, ..., lambdan
	   lambda[i] ;
	 }
	 
//return a 1Xn vector containing all eigen values
return lambda;
 }