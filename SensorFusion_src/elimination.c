double* elimination(double Z[], int m)
{
//returns a Z' containing only those sensor values whose value is supported by more than 70% sensors.

//also maintain a list of sensors whose values have been discarded
return Z'[];
}