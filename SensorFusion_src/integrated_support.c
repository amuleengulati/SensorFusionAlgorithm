double* integrated_support(double alpha[], double y[], int m)
{
	for(int i = 0;i<m;i++)
	{
		//implement a matrix multiplication operation to multipy 1Xn alpha[] and 1X n y[]
		//Z[i] = alpha[i] * y[i];
	}
	
//the function returns the 1Xm Z vector; figure out the tweak to implement this
return Z;
}